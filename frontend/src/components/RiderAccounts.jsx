import { useState } from 'react';
import { api } from '../lib/api';
export default function RiderAccounts(){const [busy,setBusy]=useState(false),[message,setMessage]=useState('');async function create(e){e.preventDefault();const form=e.currentTarget;const values=Object.fromEntries(new FormData(form));setBusy(true);setMessage('');try{await api('/commerce/riders',values);setMessage('Rider account created. They can sign in and open Deliveries.');form.reset();}catch(e){setMessage(e.message);}finally{setBusy(false);}}
return <details className="rounded-xl border bg-white p-5"><summary className="font-semibold cursor-pointer">Create a rider login</summary><p className="text-sm text-slate-500 mt-3">Create an account for an authorized rider. Restaurants can then assign orders to them at handover.</p><form onSubmit={create} className="grid sm:grid-cols-2 gap-3 mt-4"><label>Name<input className="field" name="name" required minLength={2}/></label><label>Email<input className="field" name="email" type="email" required/></label><label>Initial password<input className="field" name="password" type="password" required minLength={12} autoComplete="new-password"/></label><label>Delivery zone<input className="field" name="zone_id" required placeholder="ZONE_A"/></label><label>Vehicle<select className="field" name="vehicle"><option value="bike">Bike</option><option value="scooter">Scooter</option><option value="car">Car</option></select></label><button className="btn self-end" disabled={busy}>{busy?'Creating…':'Create rider account'}</button></form>{message&&<p role="status" className="mt-3 text-sm">{message}</p>}</details>;}
