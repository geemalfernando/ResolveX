import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { supabase } from '../lib/supabaseClient';
export default function Signup(){const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState('');const navigate=useNavigate();
 async function submit(e){e.preventDefault();setBusy(true);setMessage('');try{const {data,error}=await supabase.auth.signUp({email:email.trim(),password,options:{data:{name},emailRedirectTo:window.location.origin+'/login'}});if(error)throw error;if(data.session)navigate('/');else setMessage('Check your email to confirm your account, then sign in to place your order.');}catch(e){setMessage(e.message);}finally{setBusy(false);}}
 return <form onSubmit={submit} className="panel max-w-md mx-auto my-12 p-8 space-y-4"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-teal-700">ResolveX</p><h1 className="text-2xl font-bold tracking-tight">Your next meal starts here</h1><p className="text-slate-500">Create a customer account to order and track deliveries.</p><label className="block">Your name<input required minLength={2} className="field" autoComplete="name" value={name} onChange={e=>setName(e.target.value)}/></label><label className="block">Email<input required type="email" className="field" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block">Password<input required minLength={12} type="password" className="field" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)}/></label><p className="text-xs text-slate-500">Use at least 12 characters.</p>{message&&<p role="status">{message}</p>}<button disabled={busy} className="btn w-full">{busy?'Creating account…':'Create account'}</button><p className="text-sm">Already registered? <Link className="underline" to="/login">Sign in</Link></p></form>;
}
