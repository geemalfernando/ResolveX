"""Retired: EXTERNAL is an internal class mapped to public/persisted neither."""
if __name__ == "__main__":
    print("No migration needed. EXTERNAL predictions persist as neither; internal cause is preserved in JSON.")
